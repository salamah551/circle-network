'use client';

/**
 * PreLaunchBanner - Legacy component
 * No longer displays as all features are now available based on membership tier
 * Kept for compatibility but always returns null
 */
export default function PreLaunchBanner() {
  // All features are now available based on membership tier capabilities
  // No pre-launch banner needed
  return null;
}
